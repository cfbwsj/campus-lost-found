[phases.setup]
nixPkgs = ["python39", "tesseract", "libGL"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["mkdir -p uploads/images uploads/ocr uploads/classify"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"

